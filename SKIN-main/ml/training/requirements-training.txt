# Dépendances pour l'entraînement local (en plus de TensorFlow).
# pip install -r requirements-training.txt
tensorflow>=2.15,<3
numpy>=1.24
matplotlib>=3.8
scikit-learn>=1.3
datasets>=2.16
Pillow>=10.0
PyYAML>=6.0
